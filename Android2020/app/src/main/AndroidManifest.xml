<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muneiah.android2020">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver
            android:name=".AlarmManger.MyReceiver"
            android:enabled="true"
            android:exported="true"></receiver>

        <service
            android:name=".AlarmManger.MyAlarmService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".AlarmManger.AlarmActivity" />
        <activity android:name=".RecyclerDiffUtil.RecyclerDiffUtilActivity" />
        <activity android:name=".RoomDataBase.RoomDataBaseActivity" />
        <activity android:name=".TabNavigation.TabNavActivity" />
        <activity android:name=".NavigationComponent.NavigationActivity" />
        <activity android:name=".SharedPrefarences.LoginActivity" />

        <receiver
            android:name=".BroadCastRecevers.MyReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".InputControls.InputControlActivity" />
        <activity android:name=".RecyclerView.RecyclerViewDemo" />
        <activity android:name=".Intents.IntentsActivity" />
        <activity android:name=".text_and_scrollView.TextAndScrollViewActivity" />
        <activity android:name=".HelloToast.Hello_Toast_Class" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>